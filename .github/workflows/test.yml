name: test & lint

on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Tokyo
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 16.14.0
      - name: Install
        run: yarn
      - name: lint
        run: yarn lint
      - name: Unit test
        run: yarn test
